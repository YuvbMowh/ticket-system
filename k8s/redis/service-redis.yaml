# Redis Service（ClusterIP）：集群内通过 Service DNS "redis" 访问
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: ticket-system
spec:
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP
