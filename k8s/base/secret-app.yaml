# Secret：应用级密钥（JWT 签名密钥）
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: ticket-system
type: Opaque
stringData:
  # HS256 要求密钥 >=32 字节（消除 InsecureKeyLengthWarning）；仅演示值，生产必须轮换
  JWT_SECRET: "k8s-demo-jwt-secret-change-me-0123456789abcdef"
